# Practice 033: Calculate S(n) = sqrt(2 + sqrt(2 + ... + sqrt(2 + sqrt(2)))) with n square root signs

Calculate S(n) = sqrt(2 + sqrt(2 + ... + sqrt(2 + sqrt(2)))) with n square root signs.

Only using while loop.
